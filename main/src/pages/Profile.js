import Profile from "../components/Profile";

import React, { Profiler } from "react";

const ProfilePage = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default ProfilePage;
